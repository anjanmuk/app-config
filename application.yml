server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/anjanmuk/app-config.git #<-- CHANGE ME

security:
  basic:
    enabled: false

management:
  security:
    enabled: false
